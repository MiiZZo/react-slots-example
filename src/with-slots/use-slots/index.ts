import { attachSlots } from './attach-slots';
import { createSlot } from './create-slot';
import { useSlots } from './use-slots';

export const slotsLib = {
  useSlots,
  createSlot,
  attachSlots,
}
